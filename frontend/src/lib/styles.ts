import type { CSSProperties } from 'react'

/** Estilo do display tipográfico (Space Grotesk) usado em títulos e números. */
export const display: CSSProperties = {
  fontFamily: 'var(--display)',
  fontWeight: 700,
  letterSpacing: '-.5px',
}
